<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Ishu ❤️</title>

<style>
    body {
        margin: 0;
        height: 100vh;
        font-family: 'Segoe UI', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #fff;
        text-align: center;
        overflow: hidden;
        transition: background 1s ease;
        background: linear-gradient(135deg, #ff758f, #ffb3c6);
    }

    body.bowling-bg {
        background: linear-gradient(135deg, #6a85b6, #bac8e0);
    }

    body.cafe-bg {
        background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    }

    .container {
        background: rgba(255, 255, 255, 0.18);
        padding: 40px;
        border-radius: 22px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.25);
        width: 90%;
        max-width: 460px;
        z-index: 2;
    }

    button {
        border: none;
        padding: 15px 32px;
        font-size: 18px;
        border-radius: 30px;
        cursor: pointer;
        transition: all 0.3s ease;
        margin: 10px;
    }

    .yes {
        background-color: #ff4d6d;
        color: white;
    }

    .no {
        background-color: #555;
        color: white;
    }

    .options {
        display: none;
        margin-top: 20px;
    }

    .date-btn {
        background-color: #ff5c8a;
        color: white;
        width: 85%;
    }

    .result {
        font-size: 22px;
        line-height: 1.6;
    }

    /* 💖 Falling Hearts */
    .heart {
        position: fixed;
        top: -30px;
        font-size: 24px;
        animation: fall linear forwards;
        opacity: 0.8;
        z-index: 1;
    }

    @keyframes fall {
        to {
            transform: translateY(110vh);
            opacity: 0;
        }
    }
</style>
</head>

<body>

<div class="container" id="box">
    <h1 id="question">Hey Ishu ❤️</h1>
    <p id="subtext">Are you interested to go on a date with me? 💕</p>

    <div id="answerButtons">
        <button class="yes" id="yesBtn">Yes 😍</button>
        <button class="no" id="noBtn">No 🙈</button>
    </div>

    <div class="options" id="dateOptions">
        <h1>Choose our date 💖</h1>
        <button class="date-btn" id="bowling">🎳 Bowling Date</button><br>
        <button class="date-btn" id="cafe">☕ Board Café</button>
    </div>
</div>

<script>
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const options = document.getElementById("dateOptions");
    const body = document.body;
    const box = document.getElementById("box");

    let yesSize = 1;
    let noSize = 1;
    let heartInterval;

    noBtn.addEventListener("click", () => {
        yesSize += 0.1;
        noSize -= 0.1;
        yesBtn.style.transform = `scale(${yesSize})`;
        noBtn.style.transform = `scale(${Math.max(noSize, 0.4)})`;
    });

    yesBtn.addEventListener("click", () => {
        document.getElementById("answerButtons").style.display = "none";
        options.style.display = "block";
    });

    function startHearts() {
        heartInterval = setInterval(() => {
            const heart = document.createElement("div");
            heart.className = "heart";
            heart.innerText = "❤️";
            heart.style.left = Math.random() * 100 + "vw";
            heart.style.fontSize = (16 + Math.random() * 24) + "px";
            heart.style.animationDuration = (3 + Math.random() * 3) + "s";
            document.body.appendChild(heart);

            setTimeout(() => heart.remove(), 6000);
        }, 300);
    }

    function finalScreen(message, bgClass) {
        body.className = bgClass;
        options.style.display = "none";
        box.innerHTML = `<div class="result">${message}</div>`;
        startHearts();
    }

    document.getElementById("bowling").addEventListener("click", () => {
        finalScreen(
            "🎳 <b>Bowling Date is booked for Saturday ❤️</b><br>Can’t wait to see you, Ishu 💕",
            "bowling-bg"
        );
    });

    document.getElementById("cafe").addEventListener("click", () => {
        finalScreen(
            "☕ <b>Board Café Date is booked for Saturday ❤️</b><br>Can’t wait to spend time with you, Ishu 💖",
            "cafe-bg"
        );
    });
</script>

</body>
</html>
